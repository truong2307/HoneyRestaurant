@model IEnumerable<Honey.Web.Models.Dto.CategoryDto>

<br />
<br />
<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-info">Category List</h1>
        </div>
        <div class=" col text-end pt-1">
            <a asp-action="ProductCreate" class=" btn btn-outline-info"><i class="fas fa-plus"></i>Create New Category</a>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Options</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var category in @Model)
            {
                <tr>
                    <td>@category.Name</td>
                    <td>
                        <a asp-action="ProductEdit" class="btn btn-success" asp-route-productId="@category.CategoryId"> 
                        <i class="fas fa-edit"></i>
                    </a>
                        <a asp-action="ProductDelete" class="btn btn-danger" asp-route-productId="@category.CategoryId">
                        <i class="fas fa-trash"></i>
                    </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
